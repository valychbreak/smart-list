import TodoItem from "../todo-item-list/types";

type ExportItemListItemProps = {
    todoItem: TodoItem;
};

const ExportItemListItem = (props: ExportItemListItemProps) => {
    return (null);
}

export default ExportItemListItem;
